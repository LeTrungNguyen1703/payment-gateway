# Environment variables for Railway deployment

# Database - Railway will provide this automatically if you add a PostgreSQL service
DATABASE_URL="postgresql://user:password@host:port/database?schema=public"

# Keycloak Configuration
KEYCLOAK_REALM=your-realm-name
KEYCLOAK_CLIENT_ID=your-client-id
KEYCLOAK_CLIENT_SECRET=your-client-secret
KEYCLOAK_AUTH_SERVER_URL=your-keycloak-url

# Ably API Key - Get from https://ably.com/
ABLY_API_KEY="your-ably-api-key"

# Port - Railway automatically sets this
PORT=3000

# Redis Configuration
# Railway provides REDIS_PRIVATE_URL for internal service-to-service connections (recommended)
# REDIS_URL is for public/external connections or local development
# The app will automatically use REDIS_PRIVATE_URL if available, then fallback to REDIS_URL

# For Railway: Redis will automatically provide REDIS_PRIVATE_URL
# Example: redis://default:password@redis.railway.internal:6379
REDIS_PRIVATE_URL=

# For local development or public connections:
REDIS_URL=your-redis-url

# Legacy Redis settings (optional, kept for backward compatibility with docker-compose)
REDIS_HOST=your-redis-host
REDIS_PORT=your-redis-port
REDIS_PASSWORD=
REDIS_USERNAME=
